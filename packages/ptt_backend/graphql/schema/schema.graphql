schema {
  query: Query
}
type Query {
  petitions: [Petition!]!
  petition(id: Int!): Petition
  chart(petitionId: Int!): [PetitionTick]!
}
type Petition {
  no: Int!
  hk: Int!
  endDate: String!
  remains: String!
  category: String!
  people: String!
  ttl: Int!
  rk: String!
  title: String!
}
type PetitionTick {
  hk: Int!
  rk: String!
  people: Int!
  ttl: Int!
}
